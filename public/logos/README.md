# Company logos for Work section

Add your logo image files here. Expected names:

- `master-tour.png` (or .svg)
- `shopify.png` (or .svg)
- `godaddy.png` (or .svg)

Update the `logo.src` path in `src/pages/index.astro` if you use different filenames or extensions.
